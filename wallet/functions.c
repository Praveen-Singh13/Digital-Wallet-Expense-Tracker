#include "functions.h"
#include <stdio.h>

struct transactions TransactionList = {NULL, 0};

void addTransaction()
{
     printf("Enter the amount:\n ");
     scanf("%f", &transaction.amount);
     printf("Category:\n ");
     scanf("%s", transaction.category);
     printf("Merchant:\n ");
     scanf("%s", transaction.merchant);
     printf("Type (credit/debit):\n ");
     scanf("%s", transaction.type);
}
float calculateBalance()
{
    // TODO
}
void generateReport()
{
    // TODO
}
